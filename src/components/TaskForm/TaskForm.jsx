import styles from './TaskForm.module.css';

const TaskForm = () => {
  return <form>
    <input type="text" placeholder='Aprender React.js' />
    <button>Agregar</button>
  </form>;
};

export default TaskForm;
